// const express = require('express');
// const router = express.Router();
// const { auth } = require('../middleware/auth');
// const { getCart, addToCart, updateCartItem, removeCartItem } = require('../controllers/cart');
// // Ajoutez cette ligne avec vos autres imports de routes
// const paymentRoutes = require('./routes/payments');
// router.get('/', getCart); // Remove `auth`
// // router.get('/', auth, getCart);
// router.post('/', auth, addToCart);
// router.put('/', auth, updateCartItem);
// router.delete('/:productId/:color/:size', auth, removeCartItem);

// module.exports = router;